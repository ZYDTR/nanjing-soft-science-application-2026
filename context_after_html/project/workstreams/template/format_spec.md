# 2023 A 表第四、第五部分版式规范

原件：`2023RR软科学_A.docx`，SHA-256 `ddeff002de83f46497c4b40ebafc76b3ddf892d066c91b872eee20ce85ee762c`。数据来自原件 `word/document.xml`、`word/styles.xml`、表格 XML 与 `source_audit.json`，并用 bundled LibreOffice + 任务内 `fonts.conf` 渲染核对。渲染页码仅供定位；本机代用字体影响分页，不是 Word 中的精确页码。

| 项目 | 原件确证 | 继承/来源 |
|---|---|---|
| 页面 | 单节，Letter 8.5×11 英寸（12240×15840 twips），纵向；上下 1 英寸、左右 1.25 英寸；页眉页脚距边 0.5 英寸 | `sectPr`，无分页奇偶/首页例外；无页眉页脚部件及文字、无页码 |
| 外层结构 | 第四、第五各为“标题段 + 一行一列内容表”；标题在表外，表内一格包全部正文。表单元格被 `w:sdt` 包裹 | `w:body` 直接子节点 31/32、34/35；内容文字 `w:p` 缓存索引 145–194、195–228 |
| 表格 | 表网格、单元格宽均 8856 twips（6.15 英寸）；表宽设 auto。行最小高度 3710 twips（2.58 英寸）。四边及内边框黑色 `single`、`sz=4`（0.5 pt）。无显式底纹或单元格边距；默认内容表格样式 | 表的 `tblPr`、`tblGrid`、`trPr`、`tcPr`；宽度比页内净宽 8640 twips 多 216 twips，后续必须在目标 Word 检查贴边情况 |
| 一级标题 | “四、…”、“五、…”：15 pt 东亚黑体；黑色（未显式设色）；段落加 `keepNext`、`keepLines`，无显式段距。末尾“（文档标题）”为白色隐藏模板提示，应在正式文本处理时保留版式而移除提示文字 | 段落 `pStyle=1` → `Heading 1` → `Normal`；白色仅来自该尾随 run 的直接 `color=FFFFFF` |
| 4.1/4.2/4.3 小节 | 12 pt 加粗；4.1 数字段直接指定宋体/仿宋 GB2312 的西文/复杂脚本槽，中文由默认宋体接管；1.5 倍行距 | 普通段落，无 Heading 样式；标题格式主要来自 run/段落直接设置。4.3 另带两字符首行缩进，模板本身不完全一致 |
| 4.1.1/4.1.2 小标题 | 12 pt 加粗宋体，`outlineLvl=2`；1.5 倍行距 | 普通段落 + 直接 `outlineLvl`/run 设置；不是 Word Heading 3 |
| 正文 | 中文宋体，西文 Times New Roman；12 pt，黑色（未显式设色），两端对齐；行距 360/240=1.5 倍；首行 2 汉字（`firstLineChars=200`、通常 480 twips）；无显式段前/段后 | `docDefaults` 定义字体；Normal 定义两端对齐与 10.5 pt 缺省字号，正文 12 pt 与缩进/行距为直接设置 |
| “观点1”等行 | 两字符首行缩进；“观点N.”加粗，后续句子不加粗；12 pt、1.5 倍行距 | 同一段落的分 run 直接加粗 |
| 公式 | 第四节 77 个 `o:OLEObject`，ProgID 全为 `Equation.3`；公式行使用 `ae`（公式）或 `af`（公式据中国）样式，编号文本常为 12 pt，部分公式行设首行 420 twips/制表位。公式真实数学内容依赖 OLE 预览，不能从文本提取完整恢复 | `ae` 无父样式，缺省东亚 Times New Roman、10.5 pt；`af` 基于 `ae`。ProgID 不证明 MathType 来源。第五节无 OLE |
| 图与图注 | 第五节内联技术路线图为 `image76.png`，Word 尺寸 5486400×3403600 EMU（6×3.72 英寸）；“图2 技术路线图”居中、宋体 12 pt、行距 276/240=1.15 倍 | 图位于第五节表格内索引 211，图注索引 213；`rId160`。图像自身为绿/橙/蓝三栏，颜色属于图片，不是正文样式 |

第四节内容结构为 4.1 主要思想和观点（含 4.1.1/4.1.2 与“观点”句）、4.2 思想、4.3 理论方法与公式/解释；第五节为方案概述、5 条方法列表、技术路线图与图注。正式撰写可沿用层次与视觉节奏，但内容、公式编号及图应按 2026 项目重做。原件部分表格跨页，字体替换后的 LibreOffice 见 `reference_render_fontmapped` 页 19–27；未映射版本的 17 页出现大量缺字，不能用于格式判断。
